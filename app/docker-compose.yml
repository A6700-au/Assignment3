services:
  nginx:
    build: .
    ports:
      - "8081:80"
    develop:
      watch:
        - path: ./testfile.html
          action: rebuild
        - path: ./Dockerfile
          action: rebuild
